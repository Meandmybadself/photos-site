{
  "name": "photo-site",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "ingest": "node import.js",
    "ingest-photo": "node ingest-photo.js",
    "build": "node --env-file=.env build.js",
    "regen": "node --env-file=.env build.js --regen",
    "delete": "node --env-file=.env build.js --delete"
  },
  "dependencies": {
    "cloudinary": "^2.5.1",
    "exif-reader": "^2.0.3",
    "js-yaml": "^4.1.0",
    "sharp": "^0.33.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp"
    ]
  }
}
